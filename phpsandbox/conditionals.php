<?php

	// CONDITIONALS
	
	$num = 5;

	if($num > 4) {
		echo "$num passed";
	} else {
		echo "$num not passed";
	}

	// LOGICAL OPERATORS
	// AND && - both
	// OR || - one of them
	// XOR - one of them passes but not both
?>